1. virtual environments
    - Create: python -m venv github_api
    - Activate: source github_api/Scripts/activate
2. Libraries
3. How install Libraries
    - pip install requests
    - pip install -r requirements.txt
4. How to store secret values and prevent pushing it to git
5. Pagination: If you have lots of data in the database, we would like to fetch them in batches with a certain batch size

1. Fetch logs from cloudwatch and store them to a file
2. Deploy python script to AWS lambda to start as well stop instances